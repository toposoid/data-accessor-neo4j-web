# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
POST    /getQueryResult             controllers.HomeController.getQueryResult()
POST    /getQueryFormattedResult    controllers.HomeController.getQueryFormattedResult()
POST    /executeQuery               controllers.HomeController.executeQuery()
